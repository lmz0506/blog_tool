# 20260709-02 超时误判修复与任务30抢救发布

## 问题排查

任务 #30（排期 2026-07-09）状态为 failed，但文章文件已完整生成。运行记录（run #18）显示：

- 执行时长 **931 秒**，超过 900 秒（15 分钟）超时上限，进程被强制终止
- 但 Agent 实际已完成全部工作：文章（33KB）已写入仓库，stdout 中有完整的 `BLOG_TOOL_TASK_DONE` 结果 JSON
- 失败原因是判定逻辑要求"退出码为 0 **且**有完成标记"——超时强杀导致退出码非 0，整次运行被误判失败，提交推送环节未执行

## 修复内容

### 1. 成功判定以完成标记为准

| 文件 | 方法 | 变更 |
|---|---|---|
| `server/src/services/executorService.js` | `runExecutor` | 运行状态判定从"退出码 0 且有完成标记"改为"**stdout 中解析出 BLOG_TOOL_TASK_DONE 标记即为成功**"。Agent 打印完结果后才被超时终止的情况不再误判；文件真实性由后续的文章校验环节兜底（路径校验 + front-matter 校验） |

### 2. 默认超时提高到 30 分钟

| 文件 | 变更 |
|---|---|
| `server/src/services/storage/database.js` | 执行器种子 `timeout_ms` 900000 → 1800000；新增一次性迁移 `migrateExecutorTimeout`（仅将仍为 900000 的存量执行器提升到 1800000，用户自定义值不受影响） |

### 3. 任务 #30 抢救发布

复用工具自身逻辑（front-matter 校验 → 标记 done → 登记文章 → git 提交推送）将已生成的文章补发布：

- front-matter 校验通过（无需修补）
- 提交 `7462990 docs(DeepAgents): publish 构建第一个 DeepAgent：规划、执行与结果汇总的单智能体闭环`（968 行）并推送成功
- 任务 #30 状态修正为 done，发布结果已入库

## 验证

`node --check` 通过；文章已推送至 origin/main，GitHub Pages 自动发布。
