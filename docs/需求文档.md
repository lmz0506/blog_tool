# blog_tool 需求文档

> 本文档是 blog_tool 的需求总览，持续维护。每次需求变更/修复的详细内容见 docs 目录下对应的编号变更文档。

## 项目定位

blog_tool 是围绕 Jekyll 博客仓库（`E:\idea_space\blog`，线上地址 doc.limverse.com）构建的自动化写作工具：通过本机 CLI Agent（Codex / Claude Code）生成文章任务计划和文章正文，写入博客仓库 `_docs/<分类>/` 并 git 提交推送，由 GitHub Pages 自动发布上线。

## 技术架构

- **server/**：Express 4 + node:sqlite + simple-git + gray-matter，端口 4321，数据库 `storage/blog-tool.db`
- **web/**：React 18 + Vite 5 + Tailwind 3 单页应用，开发端口 4173
- **执行器**：spawn 本机 CLI Agent 子进程，Prompt 走 stdin/参数模板，从 stdout 提取带 `BLOG_TOOL_TASK_DONE` 标记的 JSON 结果

## 功能需求清单

### 1. 配置中心（4 个标签页）
- **仓库与发布**：博客仓库路径（目录浏览器选择 + 手动输入）、目标分支、文档目录、自动推送开关；仓库扫描（扫描 `_docs/` 生成分类与文章索引，识别目录名与 front-matter 展示分类名的差异，如 devops → DevOps）
- **自动化**：系统内置默认任务开关（从默认分类池随机选类）、默认执行器、定时调度开关（到期任务自动执行，默认关闭）
- **分类管理**：启用/停用、加入默认任务池、手动新增自定义分类
- **执行器**：名称、命令（自动扫描本机 CLI Agent / 文件浏览 / 手动输入）、工作目录（目录选择 + 手动输入）、超时时间、启用状态；行内测试按钮（测试提示词固定，不可配置）；codex 沙箱可写目录仅授予博客仓库工作目录（cwd + `{runDirectory}`），不授予工具安装目录（安装目录升级时重装会导致沙箱授权失效），参数模板不暴露在界面

### 2. 智能任务生成（草稿批次）
- 选择分类 + 执行器 + 数量 + 目标描述，调用 Agent 生成文章任务计划（标题 + 内容摘要）
- 未排期任务项自动按"今天起逐日递增"排期
- 草稿项可编辑（标题/摘要/顺序/排期）、单条删除、单条确认、勾选批量确认、整批确认
- 生成失败的批次状态标记 failed，可删除

### 3. 任务列表与执行
- 任务列表为统一看板：全部任务分页展示（每页 9 张卡片），按「执行中 → 待执行 → 已失败 → 已完成」+ 执行时间排序；分类与执行状态为筛选条件（状态选项带实时数量）
- 草稿确认后按排期日期分组生成正式任务（同日多项合并为一个任务）
- 任务执行为**队列模式**：手动执行与定时调度统一入队（新状态"已入队"，重复入队 409 去重），单一消费者按排期串行执行（同一时刻只跑一个 Agent）；队列持久化在任务表中，程序中断重启后执行中的任务自动重新入队、继续消费；已入队任务可取消排队
- **定时调度**：每 60 秒将到期（排期 ≤ 当前时间）的待执行任务入队；**默认任务为每日兜底**——永久存在，当天没有任何排期任务（不论状态）且当天未执行过时自动执行一次，执行后自动复位可复用
- 任务执行：Agent 按 Prompt 写文章到 `_docs/<目录名>/`，front-matter 使用展示分类名
- 发布前校验：文章路径必须在文档目录内（防路径穿越）、文件必须存在；front-matter 缺失字段自动补齐（layout/title/category/date/tags）
- git 发布：文章 + assets 下新增图片一并 add/commit，自动推送可配置；发布结果（提交/推送/文件清单/错误）持久化并在任务详情展示
- 失败任务可重新执行；服务重启自动恢复卡在 running 的僵尸记录
- 定时调度：开启后服务端每分钟检查执行日期已到的待执行任务，用默认执行器自动执行

### 4. 运行记录
- 每次 Agent 调用记录 Prompt/stdout/stderr/结果到 task_runs 表，UI 可查看最近 60 条日志

## 变更历史

| 日期 | 文档 | 摘要 |
|---|---|---|
| 2026-07-03 | [20260703-01-稳定性修复与发布链路补全](./20260703-01-稳定性修复与发布链路补全.md) | 修复 running 卡死、批量加入假实现、整批确认重复建任务；新增 front-matter 校验与路径防穿越、分类目录名/展示名分离、多文件 git 提交、发布结果展示、失败重试、定时调度；补全执行器编辑字段；清理死代码与博客仓库残留文件 |
| 2026-07-06 | [20260706-01-前端界面重构与配置交互优化](./20260706-01-前端界面重构与配置交互优化.md) | 前端重构为「砚台」纸墨风格，配置中心拆为 4 个标签页；新增目录/文件浏览器与执行器命令自动扫描；`--add-dir` 自动指向工具目录并隐藏参数模板；执行器测试收进行内、提示词固定；修复执行器配置被启动种子覆盖的 Bug |
| 2026-07-06 | [20260706-02-默认任务创建修复与提示信息汉化](./20260706-02-默认任务创建修复与提示信息汉化.md) | 创建默认任务前自动保存自动化配置，修复"Built-in default task is disabled"报错；系统默认任务无知识点时 Prompt 指示 Agent 自主选题；服务端业务报错全部汉化 |
| 2026-07-06 | [20260706-03-任务执行异步化与失败根因修复](./20260706-03-任务执行异步化与失败根因修复.md) | 任务执行改异步（202 + 前端 5 秒轮询）+ 进程内执行锁防重复触发；修复 dev 模式 --watch 自动重启杀死执行中任务的根因；发布连带提交范围收窄到 assets/images；时间显示统一上海时区；扫描按钮移入分类管理、所有分类默认加入默认任务池 |
| 2026-07-06 | [20260706-04-任务列表改为统一看板](./20260706-04-任务列表改为统一看板.md) | 任务列表从"分类→任务"两层改为单层看板：全部任务分页卡片展示，按状态+执行时间排序，分类与状态为筛选条件 |
| 2026-07-07 | [20260707-01-时间显示与排期编辑支持时分秒](./20260707-01-时间显示与排期编辑支持时分秒.md) | 所有时间显示完整年月日时分秒（上海时区）；排期编辑升级为 datetime-local 可精确到秒；调度器按完整时间判断到期；修正排期值被误做时区换算的问题 |
| 2026-07-08 | [20260708-01-推送状态修复手动推送与Agent效率约束](./20260708-01-推送状态修复手动推送与Agent效率约束.md) | 修复 push 失败时提交状态被误记的 Bug，推送失败原因入库并在页面展示；新增手动推送按钮；写作 Prompt 强制 UTF-8 并限制探索范围（禁读无关目录）；子进程注入 UTF-8 环境变量 |
| 2026-07-08 | [20260708-02-定时调度一轮执行全部到期任务](./20260708-02-定时调度一轮执行全部到期任务.md) | 定时调度每轮串行执行全部到期任务（原来每轮只执行最早一条），单条失败跳过继续 |
| 2026-07-09 | [20260709-01-持久化任务队列与默认任务每日兜底](./20260709-01-持久化任务队列与默认任务每日兜底.md) | 引入持久化任务队列（已入队状态、去重、串行消费、中断重启恢复），手动执行统一走队列；默认任务改为永久存在的每日兜底任务 |
| 2026-07-09 | [20260709-02-超时误判修复与任务30抢救发布](./20260709-02-超时误判修复与任务30抢救发布.md) | 修复"Agent 已完成但被超时强杀导致整次运行误判失败"的问题（成功判定以完成标记为准）；默认超时提升到 30 分钟；任务 #30 已生成的文章补发布上线 |
| 2026-07-10 | [20260710-01-移除工具目录写权限修复Codex升级后写入失败](./20260710-01-移除工具目录写权限修复Codex升级后写入失败.md) | 根因：升级重装安装目录导致 codex Windows 提权沙箱对 `{toolRoot}` 的 ACL 授权丢失且无法静默补授权，沙箱初始化整体失败、所有写入被拒；从 codex 参数模板移除 `--add-dir {toolRoot}`（种子 + 幂等迁移），并即时修补了本机已安装应用的数据库 |
| 2026-07-10 | [20260710-02-安装包版本单一来源更新自动重启与体积优化](./20260710-02-安装包版本单一来源更新自动重启与体积优化.md) | 安装包版本号以根 package.json 为唯一来源（extraMetadata + afterPack 盖章）；更新改为静默安装并自动重启（isForceRunAfter 非静默下被忽略的问题）；托盘与窗口标题显示版本号；裁剪语言包仅留中英（安装包 97MB→89MB）；确认体积主体为 Electron 运行时 |

## 已知遗留事项

1. 生成/执行接口为同步长阻塞（最长 900 秒），待改造为任务队列 + 轮询
2. 执行器命令等绝对路径依赖本机环境（可在设置页修改）
3. Windows 超时杀进程不彻底（孙进程可能残留）
4. web/dist 无 server 静态托管，生产部署方式待定
5. blog_tool 项目无 git 版本控制历史
